server:
  port: 8090

spring:
  application:
    name: msvc-shipping

  datasource:
    url: jdbc:h2:file:./data/shipping_db
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console